# Copy to .env.local for development, and set the same keys in the Amplify build.
#
# Scheme and host only, no trailing slash and no /api path — src/lib/api.ts appends
# /api/v1/... itself.
CRM_API_BASE_URL=https://your-backend-host
